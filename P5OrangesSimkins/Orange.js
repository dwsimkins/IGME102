Orange () {
    constructor () {
        this.r = (math.random()) * 30;
        this.x = (math*random())*width;
        this.y = (math*random())*height;
    }
    showCircle () {
        circle (this.x,this.y,this.r);
    }
    moveCircle () {
        
    }
}